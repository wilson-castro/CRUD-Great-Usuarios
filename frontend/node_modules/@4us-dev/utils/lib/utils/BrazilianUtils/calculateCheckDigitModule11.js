"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var calculateCheckDigitModule11 = function (value, base) {
    var values = value.split('').reverse();
    var sum = 0;
    var baseIndex = 0;
    for (var i = 1; i <= values.length; i++) {
        sum += base[baseIndex] * Number(values[i - 1]);
        baseIndex = baseIndex == base.length - 1 ? 0 : baseIndex + 1;
    }
    var checkDigit = sum % 11;
    return checkDigit == 10 ? 0 : checkDigit;
};
exports.default = calculateCheckDigitModule11;
