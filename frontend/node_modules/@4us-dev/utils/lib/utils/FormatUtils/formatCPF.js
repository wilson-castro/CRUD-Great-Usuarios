"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
var removeNonNumeric_1 = __importDefault(require("../StringUtils/removeNonNumeric"));
var isBlank_1 = __importDefault(require("../StringUtils/isBlank"));
exports.default = (function (cpf) {
    if (isBlank_1.default(cpf))
        return '';
    cpf = removeNonNumeric_1.default(cpf);
    if (cpf.length > 11)
        cpf = cpf.substring(0, 11);
    if (cpf.length > 9) {
        cpf = cpf.substring(0, 3) + "." + cpf.substring(3, 6) + "." + cpf.substring(6, 9) + "-" + cpf.substring(9);
    }
    else if (cpf.length > 6) {
        cpf = cpf.substring(0, 3) + "." + cpf.substring(3, 6) + "." + cpf.substring(6);
    }
    else if (cpf.length > 3) {
        cpf = cpf.substring(0, 3) + "." + cpf.substring(3);
    }
    return cpf;
});
