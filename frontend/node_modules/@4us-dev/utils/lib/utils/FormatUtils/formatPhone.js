"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
var removeNonNumeric_1 = __importDefault(require("../StringUtils/removeNonNumeric"));
var isBlank_1 = __importDefault(require("../StringUtils/isBlank"));
exports.default = (function (phone) {
    phone = removeNonNumeric_1.default(phone);
    if (isBlank_1.default(phone))
        return '';
    if (phone.length > 11)
        phone = phone.substring(0, 11);
    if (phone.length === 11) {
        return "(" + phone.substring(0, 2) + ")" + phone.substring(2, 7) + "-" + phone.substring(7);
    }
    else if (phone.length > 6) {
        return "(" + phone.substring(0, 2) + ")" + phone.substring(2, 6) + "-" + phone.substring(6);
    }
    else if (phone.length > 2) {
        return "(" + phone.substring(0, 2) + ")" + phone.substring(2);
    }
    return "(" + phone;
});
